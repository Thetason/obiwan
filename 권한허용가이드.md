# 🎤 오비완 마이크 권한 & 대체 플로우 가이드 (2025)

## 1. 모바일(안드로이드)
1. **설정 > 애플리케이션 > 오비완 > 권한**으로 이동합니다.
2. 마이크 권한을 **허용**으로 변경합니다.
3. 앱을 다시 열고 "녹음 시작" 버튼을 누른 후, 레코딩 단계에서 레벨 미터가 움직이는지 확인합니다.
4. 권한 허용이 어려운 경우 `Voice Journey` 녹음 화면에서 **WAV 파일 업로드** 버튼을 눌러 기존 음원으로 분석을 진행할 수 있습니다.

## 2. 모바일(iOS)
1. **설정 > 개인정보 보호 및 보안 > 마이크**로 이동합니다.
2. 오비완 앱의 토글을 켜서 마이크 접근을 허용합니다.
3. 앱을 재실행하고 녹음 버튼을 눌렀을 때 "마이크 준비 완료" 배너가 뜨는지 확인합니다.
4. iOS에서도 권한을 허용할 수 없다면, WAV 파일 업로드 플로우로 전환해 분석을 진행하세요.

## 3. 웹 브라우저(Chrome/Safari/Edge)
1. 주소창 왼쪽의 🔒 아이콘을 눌러 **마이크** 항목을 "허용"으로 변경합니다.
2. 페이지를 새로고침(F5)한 후 다시 녹음을 시도합니다.
3. Safari의 경우 권한 변경 후 전체 페이지 재로딩이 필요할 수 있습니다.
4. 브라우저 자체에서 마이크를 지원하지 않는 환경이라면, WAV 파일 업로드 모드로 전환하세요.

## 4. 데스크톱(macOS)
1. 최초 실행 시 macOS가 자동으로 마이크 권한을 요청합니다. **"허용"**을 눌러주세요.
2. 이후에도 문제가 있다면 **시스템 설정 > 개인 정보 보호 및 보안 > 마이크**에서 앱 권한을 다시 확인합니다.
3. macOS에서는 네이티브 MethodChannel을 통해 실시간 녹음을 지원합니다. 상태 배너에 "마이크 준비 완료" 문구가 표시되어야 정상입니다.

## 5. WAV 파일 업로드 대체 플로우
실시간 녹음이 제한되는 환경에서는 다음 절차로 기존 음원을 분석할 수 있습니다.
1. `Voice Journey` 녹음 화면에서 상단 배너 또는 준비 단계의 **"WAV 파일 업로드"** 버튼을 누릅니다.
2. 16-bit PCM WAV 파일을 선택합니다. (다른 형식은 현재 지원되지 않습니다.)
3. 업로드가 완료되면 자동으로 분석 단계로 이동하며, 결과 화면에 "분석 대상: 업로드된 파일명"이 표시됩니다.

## 6. 서버 상태 점검
녹음 권한이 정상인데도 분석이 동작하지 않는다면:
1. 서버 패널에서 CREPE/SPICE 상태를 확인합니다.
2. 필요 시 `./start_servers.sh`로 백엔드를 재기동하세요.
3. 자세한 로그는 `debug` 탭 또는 `logs/` 디렉터리에서 확인할 수 있습니다.
